PORT=4000
HOST=0.0.0.0
# Public URL clients use (your server IP or domain)
PUBLIC_URL=http://2.58.82.168:4000
# Comma-separated browser origins (portal, web clients)
CORS_ORIGINS=http://2.58.82.168:4000,http://localhost:4000
# Set true only for local dev if you need any origin
CORS_ALLOW_ALL=false
MONGODB_URI=mongodb://127.0.0.1:27017/safefare
JWT_SECRET=change-me-in-production-use-long-random-string
